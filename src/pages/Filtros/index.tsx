import filtros from "./filtros.json";

export default function Filtros() {
    return (
        <div>
            {filtros.map((opcao) => (
                <button>
                    
                </button>
            ))}
        </div>
    )
}